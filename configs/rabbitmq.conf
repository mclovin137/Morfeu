# Limites de memória/disco do RabbitMQ local (PRD 0002, RNF02).
# Montado em /etc/rabbitmq/conf.d/10-morfeu.conf (docker-compose.yml) —
# conf.d é aplicado por cima do rabbitmq.conf padrão da imagem.
vm_memory_high_watermark.absolute = 768MB
disk_free_limit.absolute = 512MB
