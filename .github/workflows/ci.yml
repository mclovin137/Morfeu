# CI do projeto — skeleton stack-agnóstico.
# Cada step é um placeholder verde; preencher os comandos reais quando a stack
# for definida (ver docs/roadmap.md item 2 e roles.md §2 etapa 8).
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: echo "TODO preencher quando a stack for definida (ex. mvn -B verify, npm ci && npm run build)"

      - name: Testes unitários
        run: echo "TODO preencher quando a stack for definida"

      - name: Testes de integração
        run: echo "TODO preencher quando aplicável (ex. Testcontainers, docker compose up)"

      - name: Análise estática
        run: echo "TODO preencher quando aplicável (ex. linter, SonarQube, Semgrep)"

      - name: Verificação de segurança (CVEs)
        run: echo "TODO preencher quando aplicável (ex. dependency-check, npm audit, trivy)"

      - name: Validação de migrations
        run: echo "TODO preencher quando houver banco (aplicar migrations do zero em container efêmero)"
